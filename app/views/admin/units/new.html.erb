<h2>Create unit</h2>

<%= form_for @unit, url: admin_lesson_units_path do |f| %>
  <div class="form-group">
    <%= render partial: 'form', locals: { f: f } %>
    <%= f.submit :class => "btn btn-default" %>
  </div>
<% end %>